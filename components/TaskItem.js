import * as React from 'react';
import {View} from "react-native";

const TaskItem = ({data}) => {
    //console.log(data)

    //const [task, setTask] = React.useState(data.currEvent.name);
    //const [event, setEvent] = React.useState(data.currEvent.location);

   // const handleClick = () => {
   //     console.log(curEventID);
   // }

    return (
    <View>
        
    </View>
    );
}


export default TaskItem;